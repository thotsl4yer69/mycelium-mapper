# Unused. The Secrets Gradle plugin and Gemini integration were removed; no
# environment variables are required to build or run the app. Both iNaturalist
# and Open-Meteo are public, keyless APIs. This file is kept only because the
# Android Studio template references it; you can safely delete it.

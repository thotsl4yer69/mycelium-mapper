<?xml version="1.0" encoding="utf-8"?>
<!--
  Auto-backup rules (used on API < 31). Default: back up everything the app
  stores — sightings, prefs, cached responses — all of which can be restored
  on a new device without harm.
-->
<full-backup-content />
